ex01: 전통적인 DOM API 기반의 어플리케이션

1. DOM API를 직접 자바 스크립트로 호출
2. 주로 DOM 조작에 특화된 JQuery와 같은 라이브러리를 활용한다.
3. 장점은 작성된 어플리케이션 코드의 전달이 쉽다(작성된 대로 js 파일을 HTML에 링크) -> 별다른 빌드없이 스트립트로 링크를 걸어주면 된다.
    -- 단 복잡해지면 이렇게 어플리케이션을 만들기가 힘들다.